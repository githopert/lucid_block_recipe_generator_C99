To run the program, use the following command in the Windows command prompt:

app.exe > recipes.txt

I already take into account the weight of the properties when creating the file essences.txt.

441 is the total number of items in the game = the number of rows in essences.txt.